const array = ['aunt', 'month', 'application', 'store', 'contribution', 'dressing', 'topple', 'reduction', 'entry', 'certain']
const input = 'on'
console.log(array.filter(el => el.search(input) !== -1))
